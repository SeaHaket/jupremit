# ─── Jupiter Developer Platform ───────────────────────────────────────────────
# Get your free key at https://portal.jup.ag
# Used by all /api/* routes that call api.jup.ag (quote, send, apy, lend, position).
JUPITER_API_KEY=your_jupiter_api_key_here

# ─── Solana RPC (server-side ONLY — never prefix with NEXT_PUBLIC_) ──────────
# Helius (recommended): https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
# Used by /api/balance to read on-chain USDC balances.
# Falls back to the public Solana mainnet endpoint if not set.
RPC_URL=https://api.mainnet-beta.solana.com

# ─── Solana RPC (browser/client — key-less public endpoint ONLY) ──────────────
# Optional. WalletProvider uses this for transaction submission and confirmations.
# Falls back to the public Solana mainnet endpoint if not set.
# Do NOT put a secret API key here — this value ships to the browser bundle.
# NEXT_PUBLIC_RPC_ENDPOINT=https://api.mainnet-beta.solana.com

# ─── Network ──────────────────────────────────────────────────────────────────
NEXT_PUBLIC_NETWORK=mainnet-beta

# ─── App URL (used for CORS — set to your deployed domain) ───────────────────
NEXT_PUBLIC_APP_URL=https://jupremit.vercel.app

# ─── Protocol Fee Wallet ──────────────────────────────────────────────────────
# Receives the 0.20% protocol fee on every send (bundled in the SPL transfer tx).
# NEXT_PUBLIC_FEE_WALLET ships to the browser — use a dedicated fee-collection wallet.
# Falls back to empty string if not set (fee instruction skipped silently).
NEXT_PUBLIC_FEE_WALLET=your_fee_wallet_pubkey_here
# FEE_WALLET is the server-side alias used by /api/time-send/release.
FEE_WALLET=your_fee_wallet_pubkey_here

# ─── Upstash Redis (rate limiting) ────────────────────────────────────────────
# Create a free Redis database at https://console.upstash.com
# Copy the REST URL and token from the database dashboard.
# If not set, rate limiting is skipped (fine for local dev).
UPSTASH_REDIS_REST_URL=https://YOUR_DB.upstash.io
UPSTASH_REDIS_REST_TOKEN=your_upstash_token_here
